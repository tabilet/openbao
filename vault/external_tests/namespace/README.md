## external_tests/namespace

Test namespace features in Openbao, across auth methods _approle_, _token_ and _userpass_, and backends _file_, _RAFT_ and _in-memory_. 

Before running the tests, make sure:
```
export VAULT_NAMESPACE=""
```
